// 初始化广告倒计时总时长
// function setTimeFormat (time) {
//   let endTime;
//   if (time) {
//       if (time > 59) {
//           let n = Math.floor(time/60);
//           let m = time - n * 60;
//           n = n < 10 ? '0' + n : n;
//           m = m < 10 ? '0' + m : m;
//           endTime = `${n}:${m}`;
//       } else if (time < 10) {
//           endTime = `00:0${time}`
//       } else {
//           endTime = `00:${time}`;
//       }
//   } else {
//       endTime = `00:00`;
//   }

  
// }

